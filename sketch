item is a pure virtual class with ID field

enum class sport {SPORT1, SPORT2}


Node<class T>
int height
Node* left, right
Node* parent
+setID(int id) // optional
+getID(int id)

Teams -> Team
Team:
int numberOfContestants
country*
allContestants*


Country:
int numberOfTeams;
allTeams*

Contestants:
strength
team*
//country*

Tree<class T>
int size;


needed functions:
    shared_ptr<T> Tree<T>::find(int id)
    returns a pointer to Item T
    if the tree does not find such id, the function returns nullptr
